<%= form_with(url: sign_in_path, scope: :session, local: true) do |form| %>
<div class="actions">
    <%= form.label :name %>
    <%= form.text_field :name %>

    <%= form.submit "Sign in", name: 'sign_in'%>
</div>
<% end %>